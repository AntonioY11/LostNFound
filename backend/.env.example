# Copy this file to `backend/.env` and fill real values there.
# IMPORTANT: Do NOT commit `backend/.env` or any file that contains real
# secrets or private keys. `.gitignore` already excludes `.env` files.

# Server
PORT=3001
JWT_SECRET=replace-me-with-a-secure-secret

# AWS (optional): if present, uploads will be pushed to S3
# Use IAM instance profile on production (recommended) rather than embedding
# long-lived credentials in a file. If you use keys here, keep them private.
AWS_ACCESS_KEY_ID=your-key
AWS_SECRET_ACCESS_KEY=your-secret
AWS_REGION=us-east-1
S3_BUCKET=your-bucket-name

# RDS / MySQL configuration (example):
# DB_CLIENT=mysql
RDS_HOST=your-rds-host.rds.amazonaws.com
RDS_PORT=3306
RDS_USER=your_db_user
RDS_PASSWORD=your_db_password
RDS_DATABASE=your_database_name
# Optional: connection pool size used by backend (`DB_POOL_SIZE`). Default: 10
# DB_POOL_SIZE=10

# Frontend URL (set this to your Elastic Beanstalk domain or production frontend URL)
# Example: FRONTEND_URL=https://Lostfound-env.eba-3ddbyeim.us-east-1.elasticbeanstalk.com
FRONTEND_URL=
# If you build a Vite frontend, set `VITE_API_BASE_URL` in the frontend build env to the same value.
# If using IAM authentication for RDS, follow AWS docs for IAM DB auth and
# configure your DB client accordingly. Do NOT commit real credentials.
